<%= form_for(@chetan) do |f| %>
  <% if @chetan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chetan.errors.count, "error") %> prohibited this chetan from being saved:</h2>

      <ul>
      <% @chetan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :commit_id %><br>
    <%= f.number_field :commit_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
