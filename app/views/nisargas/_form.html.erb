<%= form_for(@nisarga) do |f| %>
  <% if @nisarga.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nisarga.errors.count, "error") %> prohibited this nisarga from being saved:</h2>

      <ul>
      <% @nisarga.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
